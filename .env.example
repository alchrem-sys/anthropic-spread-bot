TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
REDIS_URL=        # optional — strongly recommended on Railway (Upstash free tier)
DATA_PATH=        # optional — path for config.json fallback (e.g. /data/config.json with Railway volume)
